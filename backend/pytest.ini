[pytest]
testpaths = tests
pythonpath = .
addopts = -q -p no:cacheprovider
markers =
    postgres: integration tests that require a live PostgreSQL database
filterwarnings =
    ignore::DeprecationWarning
